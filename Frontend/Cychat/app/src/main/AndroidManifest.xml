<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:name="com.example.CyChat.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">

        <!-- android:networkSecurityConfig="@xml/network_security_config"> -->
        <activity android:name="com.example.CyChat.Activity.MainPageActivity" />
        <activity android:name="com.example.CyChat.Activity.RegistrationActivity" />
        <activity android:name="com.example.CyChat.Activity.AddFriendsActivity" />
        <activity android:name="com.example.CyChat.Activity.PersonActivity" />
        <activity android:name="com.example.CyChat.Activity.FriendRequestActivity"/>
        <activity android:name="com.example.CyChat.Activity.AcceptOrNotActivity"/>
        <activity android:name="com.example.CyChat.Activity.PostStoryActivity"/>
        <activity android:name="com.example.CyChat.Activity.LoginActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>